'use client'
import { ReactNode } from 'react'
export default function SmoothScrollWithLocomotive({ children }: Readonly<{ children: React.ReactNode }>) {
    return (
        <>
            {children}
        </>
    )
}